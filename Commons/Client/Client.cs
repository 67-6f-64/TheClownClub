using System.Collections.Generic;

namespace Common {
    class Client {
        private readonly List<Bot> _bots;

        public Client() {
            foreach (var bot in _bots) {
                //bot.getBotType
            }
        }
    }
}
