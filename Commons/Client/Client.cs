using Commons.Client.Bot;
using System.Collections.Generic;

class Client
    {
        private readonly List<Bot> _bots;

        public Client()
        {

        //Map each bot to the ui.
        foreach (Bot bot in _bots)
            {
                //bot.getBotType
            }
        }
    }
